naftiko: 1.0.0-alpha2
info:
  label: Wrike API — Access Roles
  description: 'Wrike API — Access Roles. 1 operations. Lead operation: List Access Roles. Self-contained Naftiko capability
    covering one Wrike business surface.'
  tags:
  - Wrike
  - Access Roles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WRIKE_API_KEY: WRIKE_API_KEY
capability:
  consumes:
  - type: http
    namespace: wrike-access-roles
    baseUri: https://www.wrike.com/api/v4
    description: Wrike API — Access Roles business capability. Self-contained, no shared references.
    resources:
    - name: access_roles
      path: /access_roles
      operations:
      - name: listaccessroles
        method: GET
        description: List Access Roles
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WRIKE_API_KEY}}'
  exposes:
  - type: rest
    namespace: wrike-access-roles-rest
    port: 8080
    description: REST adapter for Wrike API — Access Roles. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/access-roles
      name: access-roles
      description: REST surface for access_roles.
      operations:
      - method: GET
        name: listaccessroles
        description: List Access Roles
        call: wrike-access-roles.listaccessroles
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: wrike-access-roles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Wrike API — Access Roles. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-access-roles
      description: List Access Roles
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: wrike-access-roles.listaccessroles
      outputParameters:
      - type: object
        mapping: $.
